<?php

class FosterHomeSettingsTest extends \PHPUnit\Framework\TestCase
{
public function insertChild(){
$this->FosterHomeSettings->Name('live');
$this->FosterHomeSettings->ID('8');
$this->FosterHomeSettings->regitration_ID(889);
$this->FosterHomeSettings->foster_username('live');
$this->FosterHomeSettings->password('7dhui');

$this->assertEquals($FosterHomeSettings->Name(), 'live');
$this->assertEquals($FosterHomeSettings->ID(), '8');
$this->assertEquals($FosterHomeSettings->regitration_ID(), 889);
$this->assertEquals($FosterHomeSettings->foster_username(), 'live');
$this->assertEquals($FosterHomeSettings->password(), '7dhui');
}
}
